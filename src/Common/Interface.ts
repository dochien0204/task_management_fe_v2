export interface Company {
  description: string;
  id: number;
  name: string;
}

export interface Group {
  code: string;
  id: number;
  name: string;
}

export interface Role {
  code: string;
  id: number;
  name: string;
}

export interface Status {
  code: string;
  id: number;
  name: string;
}
